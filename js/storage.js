const K="leapGoV3";const base={plays:0,answered:0,correct:0,perfect:0,great:0,maxCombo:0,xp:0,words:{},missions:{date:"",p:0,c:0,f:0},settings:{sound:"on",motion:"high",count:10,theme:"dark"}};export function data(){try{return {...base,...JSON.parse(localStorage.getItem(K)||"{}")}}catch{return structuredClone(base)}}export function save(x){localStorage.setItem(K,JSON.stringify(x))}export function answer(word,ok,type,time){let d=data();d.answered++;if(ok)d.correct++;if(type==="PERFECT")d.perfect++;if(type==="GREAT")d.great++;d.words[word]??={c:0,w:0,time:0};d.words[word][ok?"c":"w"]++;d.words[word].time+=time;let gain=ok?(type==="PERFECT"?30:20):3;d.xp+=gain;daily(d,ok,type);save(d);return d}export function combo(n){let d=data();d.maxCombo=Math.max(d.maxCombo,n);save(d)}export function daily(d,ok,type){let today=new Date().toISOString().slice(0,10);if(d.missions.date!==today)d.missions={date:today,p:0,c:0,f:0};if(ok)d.missions.c++;if(type==="PERFECT")d.missions.p++;d.missions.f=Math.max(d.missions.f,0)}export function level(d=data()){return Math.floor(d.xp/500)+1}export function title(d=data()){let l=level(d);return l>=30?"LEAP MASTER":l>=20?"WORD HUNTER":l>=10?"WORD PLAYER":"BEGINNER"}export function settings(){return data().settings}
